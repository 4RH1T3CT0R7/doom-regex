# Дизайн v2.0: фикс-адресный космос (#N + абсолютные смещения зон)

## Мотивация

Марафон-5b (полный кадр, 13.99М подстановок, 4.71ч): load_hit 45.6% +
store_hit 36.7% времени — ленивые ячеечные сканы зоны #M (~60МБ,
~3.3М ячеек). dspan_px/dcol_px (15%) тоже делают #M-lookup'ы текселей.
Цель: O(1) доступ к памяти -> кадр за ~15-30 мин, клип G2e из 100+
кадров становится реальным.

## Формат строки v2.0

```
RVM1|ST:run|PH:0|CI:<12>|PC:<8>|R0:<8>..R7:<8>|CLK:<8>|
  {мелкие ROM-таблицы #D#Q#L#A#S#B#O#X#H#T#G#J}
  #N<8 hex x 0x700000 слотов>          — 58.72МБ, ФИКС
  #P<23 x 0x100000 слотов>             — 24.12МБ, ФИКС (паддинг I..:ff..)
  #F<[oooo:vv] x 64000>                — 576КБ, ФИКС
  #W<[ppppp:32hex] x pages(4.2МБ WAD)> — 8.4МБ, ФИКС для прогона
  #M<[aaaaaaaa:vvvvvvvv]...>           — переменная (highmem: стек и пр.)
  #E|IN:<hex>|OUT:<hex>|Z
```

Ключевые инварианты:
- Заголовок до первой ROM-таблицы имеет ФИКСИРОВАННУЮ длину на
  PH:0/1/2-границах (транзиентное |MF: живёт только внутри PH:3/4 —
  правила микрофаз работают в пределах заголовка и не прыгают в зоны;
  если прыгают (dv*-таблицы) — это мелкие ROM в фикс-окне тоже).
- Все смещения OFF_N, OFF_P, OFF_F, OFF_W, OFF_M — КОНСТАНТЫ isa.py,
  известные генератору правил. Правила остаются
  программонезависимыми: #P паддится пустышками (op=ff BADOP-слоты)
  до ровно 16^5 слотов; программа обязана быть < 16^5 инструкций.
- IN/OUT в хвосте: их переменная длина больше не сдвигает зоны.
  putc: dotall-прыжок невозможен (OUT за переменным #M) — но
  putc/getc НЕ горячие (кадр в #F, не в OUT) -> HOP-скан по хвосту
  допустим; вставка в хвост дешёвая для splice (короткий не-префикс?
  нет: splice переписывает [0,mend) — mend до конца OUT = вся строка!
  РЕШЕНИЕ: prefix-skip в драйвере (см. ниже) симметрично работает и
  как «общий хвост»? Нет — только префикс. GETC/PUTC редки в кадре
  (весь IO-лог уже напечатан) — принять цену; альтернатива: OUT
  оставить в заголовке, а фикс-прыжки строить от конца OUT нельзя.
  => IN/OUT в хвост, PUTC платит полную пересборку — редкий.
- Зона #N плотная позиционная: слот = 8 hex, адрес имплицитен
  (позиция = OFF_N + 2 + 8*addr). Диапазон [0, 0x700000): данные
  программы + zone-память Doom. Highmem (стек ELVM 0xffffxx и пр.,
  всё вне #N/#F/#W-диапазонов) остаётся в #M (маленькая: сотни ячеек).

## Правила (genpattern)

- Дерево адреса как в fetch: при захвате 8 цифр адреса атомарно
  ставятся бинарные маркер-группы (?>(?=[класс])(?<qb>)|) по битам
  цифр a5..a0 (23 бита; гейт диапазона: a7=0, a6=0, a5 in [0-6]);
  прыжок = .{OFF_N+2} + сумма (?(qb)(?s:.{8*2^b})|).
- load_n (op 0x20, addr<0x700000): lookahead к слоту, захват 8 hex,
  запись в Rd. Всегда hit (плотный массив).
- store_n (op 0x22): консьюм до слота через ${pre}, замена 8 симв —
  длиносохраняющая. ВАЖНО: prefix-skip в драйвере обязателен, иначе
  рендер replacement копирует до 80МБ префикса на каждый STORE.
- push/pop/подобные — те же два пути по диапазону.
- dspan_px/dcol_px: тексель-lookup'ы (src/colormap из zone-памяти)
  переключить с #M-скана на #N-позицию (то же дерево).
- fetch: прыжок .{OFF_P+2} + существующее дерево цифр PC (HOP P
  заменяется фикс-прыжком).
- store_fb/load_fb: .{OFF_F+2} + дерево 4-значного оффсета x9 симв
  ячейки [oooo:vv] (ячейки остаются с адресами для вьювера/дампов).
- WAD: .{OFF_W+2} + постраничное дерево (замена HOP W).
- Правила #M (highmem): HOP M сканировал бы #N+#P+... -> тоже
  фикс-прыжок .{OFF_M_START}, дальше прежний ячеечный скан (мал).
  OFF_M_START фикс, т.к. #W фикс для прогона: WAD-размер — параметр
  генерации (как FB_W/H сейчас); честность не страдает (правила
  фиксируются и хешируются до запуска).
- getc/putc/echo: IN/OUT в хвосте — правила переписать на скан от #E.

## Драйвер

- prefix-skip в splice_apply: если toks[0] — группа с ov[0]==0
  (префикс всего матча), НЕ рендерить её в rbuf и НЕ копировать:
  memcpy только хвоста replacement по смещению len(pre). Семантика
  идентична (байты префикса равны сами себе), сверка pure-режимом.
- echo_out: OUT теперь в хвосте — искать "|OUT:" с КОНЦА (memrchr-
  цикл) или от OFF_M... OUT после #M: искать от конца строки.

## refemu / statecodec

- refemu: семантика НЕ меняется (ram dict как был).
- encode: новая раскладка (это единственный источник формата);
  decode_head: заголовок тот же до CLK, IN/OUT парсить из хвоста.
- rvs2state: без изменений (использует encode).

## Тесты

- NRAM_TOP/PROG_SLOTS/WAD_PAGES — параметры генерации из isa.py;
  тестовый профиль (env RVM_TEST_PROFILE=small) генерит малые зоны
  (NRAM_TOP=0x1000, PROG_SLOTS=0x1000, WAD 4КБ) — lockstep-сьют
  быстрый; боевой профиль — дефолт. Структура правил идентична,
  меняются только константы прыжков.
- Смоук боевого профиля: изолированные property-тесты load_n/store_n/
  fetch на синтетических состояниях боевого размера (по образцу
  fetch-теста, 100+ случайных адресов/PC, PCRE2 vs python-regex).
- Полный lockstep малого профиля + drivers-agree + голдены.
- Финальная сверка: refemu-прогон -> новый снапшот FB_BEGIN -> rvm.exe
  хвост -> sha кадра == 32b6e9e0 (эталон неизменен: семантика машины
  не тронута, только раскладка строки).

## Оценка

- Строка: 90.7 -> ~93МБ (#N 58.7 + паддинг #P 2.2 - усохший #M ~60МБ
  -> примерно то же).
- Скорость: load/store/texel O(1) (микросекунды), фикс-прыжки без
  сканов. Ожидание: 825 -> 5000-15000 pps на хвосте кадра.
- Риски: (1) объём правок genpattern (все HOP-идиомы горячих правил);
  (2) PUTC-пересборка — редкая, но в клипе G2e putc нет вообще;
  (3) паддинг-слоты #P — fetch на мусорный PC теперь находит ff-слот
  (BADOP-трап) вместо NOSLOT — семантику refemu согласовать (сейчас
  refemu: pc вне prog -> NOSLOT; станет: ff -> BADOP. Либо паддить
  специальным opcode-значением, которое refemu тоже трактует как
  NOSLOT для pc >= len(prog)... проще: refemu уже BADOP на ff?
  выяснить и согласовать до реализации).
